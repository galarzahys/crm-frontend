/**
 * Entorno de desarrollo (el que usa `ng serve`). Apunta al backend
 * NestJS corriendo en local (`npm run start:dev` en gestion-app-api).
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
};
